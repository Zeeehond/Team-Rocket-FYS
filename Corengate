
class Lanes {

 
    void draw() {
      background(0);
      int m = millis() /100;
      y+=speed;
      y%=height;
      image(d, 300, int(y));
      image(d.get(0, d.height-int(y), d.width, int(y)), 300, 0);
      
      text("score: " + m, 10, 50);
    }
}
